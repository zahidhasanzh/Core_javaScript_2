//Reserved keywords as of ECMAScript 2015

/*break case catch class const continue debugger default delete do else
export extends finally for function if import in instanceof new return
super switch this throw try typeof var void while with yield
 */

//These are always reserved:
//enum

//The following are only reserved when they are found in strict mode code
/*implements
interface
let
package
private
protected
public
static */

//The following are only reserved when they are found in module code:
//await

/* The following are reserved as future keywords by older ECMAScript 
specifications (ECMAScript 1 till 3). */
/* abstract
boolean
byte
char
double
final
float
goto
int
long
native
short
synchronized
throws
transient
volatile */

//Additionally, the literals null, true, and false cannot be used as identifiers in ECMAScript.

/* A few identifiers have a special meaning in some contexts without 
being keywords of any kind. They include: */
/*arguments
get
set */


//JAVASCRIPT NAMING CONVENTIONS: VARIABLES
/* JavaScript variables are case sensitive. Therefore, JavaScript
 variables with lowercase and uppercase characters are different: */
 var mathNumber = 10;
 var accountNumberDetailsId =105
 var account_number_details_id =104